function estaEnRango(num) {
  // Retorna true si "num" es menor que 50 y mayor que 20.
  // De lo contrario, retornar false.
  // Tu código:
   // Verificamos si el número está en el rango entre 20 y 50
  if (num > 20 && num < 50) {
    return true;
  }
  return false;
}
module.exports = estaEnRango;
}
module.exports = estaEnRango;
